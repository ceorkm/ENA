# ── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# ── Rust / Tauri ───────────────────────────────────────
target/
ena-app/src-tauri/target/
**/*.rs.bk
*.pdb

# ── Node ───────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*

# ── Editors ────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Tooling / scratch ──────────────────────────────────
.playwright-mcp/
*.log
*.tmp

# ── Root-level scratch: previews, captures, exports ────
# Real pages live in landing/; real assets live in media/ or ena-app/.
# Anything loose at the repo root is scratch and stays out of git.
/*.html
/*.png
/*.jpeg
/*.zip
/*.mp4
/*.mov
/*.gif
Screenshot*.png
Cap *.mp4

# ── Build artifacts produced by your machine ───────────
ena-app/src-tauri/gen/
*.app
*.dmg

# Landing site kept out of the app repo
landing/
media/
